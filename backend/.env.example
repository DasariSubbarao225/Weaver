# Backend Environment Variables
# Copy this file to .env for local development (optional)

# Node environment (development or production)
NODE_ENV=development

# Server port (default: 3000)
PORT=3000

# Allowed CORS origins (comma-separated, no spaces)
# For development: http://localhost:8000,http://127.0.0.1:8000
# For production: https://yourdomain.com,https://dasarisubbarao225.github.io
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Note: In production, these should be set as environment variables in your hosting platform
# (Render, Railway, Heroku, etc.) rather than using a .env file
